# /ralph-loop completeness — Phase R4

- **Phase:** R4 — Implement the chosen pratyaksha mechanisms in AEO
- **Date:** 2026-04-25
- **Plan:** `aeo_review_remediation_v02` → R4
- **Verdict:** ✅ complete

## Phase R4 acceptance checklist

| requirement (from plan §R4) | status | evidence |
| --- | --- | --- |
| Register `pratyaksha` in `.mcp.json` and `plugin/mcp.json` (PEP 723 `uv run` of `tools/pratyaksha/mcp/server.py`) | ✅ | Both files updated to point at the vendored submodule via `uv run --no-project`. Pratyakṣa added as a git submodule at `tools/pratyaksha`. |
| `plugin/scripts/aeo-sakshi.sh`: on `SessionStart`, call `pratyaksha.set_sakshi` with AEO invariants | ✅ | Embedded Python launches `uv run server.py`, performs MCP handshake, calls `set_sakshi` with the five AEO invariants (subscription-only Claude CLI; audit precedes brief; brief precedes PR; no synthetic citations; never overwrite — sublate). Wired in `plugin/hooks/hooks.json` `SessionStart`. Fail-open per pratyaksha convention (logs to stderr, exits 0). |
| Wrap brief generation in `packages/cli-worker/src/runners/loop.ts` with Manas/Buddhi | ✅ | Loop emits `manas` (drafting via `generate_brief`), then `buddhi` (gate). Buddhi calls `pratyaksha.context_retrieve` against `<site_id>::<page_url>` × tactic × `tier=…` bucket, classifies prior elements by precision vs `briefPrecision = clamp(predicted_lift_pp / 20)`, and decides per brief: low-precision priors → `sublate_with_evidence`; high-precision priors → block PR; no conflict → `context_insert`. |
| PR open is gated on Buddhi clearance | ✅ | When all briefs are blocked the runner returns `next_step: "buddhi_blocked"` and never calls `open_pr`. New `buddhi_blocked` outcome added to `LoopRunnerResult["next_step"]` union. |
| `mcp/test/pratyaksha.integration.test.ts` covering the witness + sublation path | ✅ | `mcp/tests/pratyaksha.integration.test.ts` (matches existing `mcp/tests/` convention) spawns the live Python pratyaksha server through stdio, performs the MCP handshake, and asserts: `set_sakshi` accepts the AEO invariant; `context_insert` of an older low-precision brief lands; `sublate_with_evidence` with strictly higher precision succeeds; `context_retrieve` makes the older element retrieval-invisible; non-strict precision rewrite is rejected; `get_sakshi` round-trips the invariant. Skips cleanly when `uv` or the submodule are missing. |
| Run `/ralph-loop`. Commit + push. | ✅ | This file + commit. |

## Test evidence

```
$ node --experimental-strip-types --test \
    mcp/tests/integration.http.test.ts \
    mcp/tests/pratyaksha.integration.test.ts \
    packages/cli-worker/src/runners/loop.test.ts

✔ HTTP: tools/list returns all 16 tool names
✔ HTTP: read-side tools roundtrip the {ok,value} envelope
✔ HTTP: emit_schema produces valid Article JSON-LD
✔ HTTP: track_citations + compare_competitors aggregate correctly
✔ HTTP: read_repo_metadata + read_results work over the wire
✔ HTTP: unknown tool returns -32601
✔ pratyaksha integration: witness + sublation path matches AEO contract  ← live cross-process
▶ runLoopOnce
  ✔ happy path: filters Tier-1 gaps above threshold, drafts briefs, opens PR
  ✔ returns no_qualifying_gaps when nothing passes the filter
  ✔ respects max_gaps_per_pr cap from SiteConfig
  ✔ falls back to site_url when seed_pages is empty
  ✔ pratyaksha first-run: inserts every brief into the Avacchedaka store
  ✔ pratyaksha sublation: lower-precision prior recommendation is superseded
  ✔ pratyaksha buddhi block: higher-precision prior keeps PR closed
  ✔ noop pratyaksha gracefully degrades when daemon was started without it
  ✔ audits every seed_page (multi-page audit)
✔ tests 16  pass 16  fail 0
```

The pratyaksha integration test was wired into `.github/workflows/ci.yml` so
this contract is enforced on every PR.

## Files added / modified

- `tools/pratyaksha` — new git submodule pinning the vendored Pratyakṣa MCP server.
- `.mcp.json` + `plugin/mcp.json` — register `pratyaksha_mcp`.
- `plugin/scripts/aeo-sakshi.sh` + `plugin/hooks/hooks.json` — `SessionStart` Sākṣī hook.
- `packages/cli-worker/src/pratyaksha_client.ts` — `PratyakshaClient` interface, `NoopPratyakshaClient` (fail-open), `StdioPratyakshaClient`, `makePratyakshaClient` factory.
- `packages/cli-worker/src/runners/loop.ts` — Manas (`generate_brief`) + Buddhi gate (`context_retrieve` → `sublate_with_evidence` / `context_insert` / block) + `buddhi_blocked` next_step + Buddhi metrics in `LoopRunnerResult` and PR body.
- `packages/cli-worker/src/runners/loop.test.ts` — four new tests covering first-run insert, sublation, block, and noop fallback.
- `mcp/tests/pratyaksha.integration.test.ts` — live cross-process integration test for the witness + sublation contract.
- `.github/workflows/ci.yml` — added pratyaksha integration tripwire job step.

## Off the critical path (deferred to later phases)

- The Khyātivāda classifier and Budget gauge stayed out of the loop per the R3
  decision; they remain available via the registered pratyaksha MCP for any
  future rule that needs them.
- The `StdioPratyakshaClient` is single-shot per call (process-per-call) so each
  Buddhi gate call is independent. Persistent state for the AEO loop will live
  in `data/sites/<site_id>/pratyaksha-store.jsonl` once R5's live run starts
  exercising it; today the Buddhi gate consults pratyaksha as a stateless
  oracle, which is what the loop runner's logic depends on.

## Ready for R5

R5 will perform the actual end-to-end live run against
`sharathsphd.github.io`: bootstrap a site config, run `/aeo:audit` →
`/aeo:fix`, and capture the real PR URL plus full transcript. The
witness + sublation gate is now wired and tested, so R5 is the first
phase where it gates a real PR.
